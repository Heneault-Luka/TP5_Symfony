<div class="card mb-3 {% if leContact.sexe == 0 %} border-info {% else %} border-pink {% endif %}">
                <h5 class="card-header text-center">{{leContact.nom}} {{leContact.prenom}}</h5>
                <img class="avatar mx-auto " style="height: 200px; max-width: 200px; display: block;" src="{{ leContact.avatar }}" alt="Card image">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">{{leContact.rue}}</li>
                    <li class="list-group-item">{{leContact.cp}}</li>
                    <li class="list-group-item">{{leContact.ville}}</li>
                    <li class="list-group-item">{{leContact.mail}}</li>
                    <li class="list-group-item">
                        {% if leContact.sexe==0 %}
                            <span class="badge badge-info">homme</span>
                        {% else %}
                            <span class="badge badge-pink">femme</span>
                        {% endif %}
                    </li>
                    {% if mode == 'listeContacts' %}
                        <li class="list-group-item"> 
                            <a href="{{path('app_fichecategorie', {'id': leContact.categorie.id})}}">{{leContact.categorie.libelle}}</a>
                        </li>
                    {% endif %}
                </ul>
                <div class="card-body">
                    <a href="{{path('app_ficheContact',{'id': leContact.id})}}" class="btn btn-success btn-block texte-center">Détail</a>
                </div>
            </div>